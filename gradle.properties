version = 2.0.0
ideaVersion = IC-2022.2
customUtilBuild = 299.*
isEAP = false
pluginChannels = nightly

# Disable the gradle-intellij-plugin self-update check (queries GitHub on
# initializeIntelliJPlugin and NPEs when the request can't be resolved).
org.jetbrains.intellij.buildFeature.selfUpdateCheck = false
